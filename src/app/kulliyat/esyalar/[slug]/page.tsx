import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, Shield, Sparkles, Sword, TestTube } from "lucide-react";
import { getItem, getItems } from "@/lib/items";
export function generateStaticParams(){return getItems().map((item)=>({slug:item.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const item=getItem((await params).slug);return {title:item?.name??"Eşya"}}
export default async function ItemDetail({params}:{params:Promise<{slug:string}>}){const item=getItem((await params).slug);if(!item)notFound();const Icon=item.category==="weapon"?Sword:item.category==="armor"?Shield:item.category==="consumable"?TestTube:Sparkles;return <main className="lore-detail shell"><Link className="back-link" href="/kulliyat/esyalar"><ArrowLeft size={14}/> Eşyalara dön</Link><section className="item-detail-hero"><div className="item-detail-mark"><Icon size={58}/></div><div><p className="eyebrow">{item.rarity} · {item.sourceSet}</p><h1>{item.name}</h1><p>{item.effect}</p></div></section><div className="lore-detail-layout"><aside className="lore-facts"><div><span>Tür</span><strong>{item.classification||item.category}</strong></div><div><span>Kullanım</span><strong>{item.mode==="active"?"Aktif":"Pasif"}</strong></div><div><span>Şarj</span><strong>{item.charges==="proficiency"?"Uzmanlık bonusu kadar":item.charges??"Sınırsız / sürekli"}</strong></div><div><span>Kaynak</span><strong>{item.source}</strong></div><div><span>Kural Kimliği</span><strong>{item.id}</strong></div></aside><article className="lore-prose"><section><h2>Etki</h2><p>{item.effect}</p></section><section><h2>Etiketler</h2><p>{item.tags.join(" · ")||"Etiket bulunmuyor."}</p></section></article></div></main>}
